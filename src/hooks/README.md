# Custom React Hooks

This directory will contain custom React hooks for audio engine integration.

## Planned Hooks

- `useAudioEngine.ts` - Hook for accessing audio engine instance
- `useTransport.ts` - Hook for transport controls
- `useMidiRecorder.ts` - Hook for MIDI recording functionality

